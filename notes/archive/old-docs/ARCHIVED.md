# Archived: the superseded `docs/` tree

This was the pre-`docs-source/` documentation tree (it lived at the repository root as `docs/`). It is a
superseded copy: its master, `index.emd`, was left empty, so it was never built, served, or treated as a
source of truth. Archived 2026-06-26.

The live documentation source is `docs-source/` (rendered by the docs-site build). Nothing here reflects the
current documentation; it is kept for historical reference only.
