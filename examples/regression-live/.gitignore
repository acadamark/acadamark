# Copied package assets — build output of `enscribe build --live` (the build helper resolves
# them from @enscribejs/enscribe and copies them flat). They are NOT source; regenerate them:
#   enscribe build --live article.emd -o .     (or run packages/cli/src/build-live.js)
# The committed source is the .emd article + the emitter-generated index.html + this README.
/enscribe.browser.global.js
/default.css
/enscribe-shell.css
/editor-codemirror.js
