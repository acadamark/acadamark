Post-v0.2.0 ship: mark shipped and finish the @enscribejs/enscribe doc sweep

v0.2.0 is published to npm (the three scoped packages). This finishes the
release on the documentation side:

- ROADMAP: v0.2.0 marked shipped and v0.3.0 set as the current milestone; the
  leftover conda-forge mention in "Current position" dropped (it is v0.4.0
  work); the released packages named by their scoped names.
- STATUS: coordinated v0.2.0 versioning.
- Coherence sweep: the remaining `enscribe/core` / `enscribe/parser` references
  (the old, now-unscoped import paths) rewritten to `@enscribejs/enscribe/core`
  / `@enscribejs/enscribe/parser` across the specs, code comments, and the
  layer1-vocabulary element docs; data.js regenerated from the element docs.
  The historical archive and the lockfile were left untouched.

Output-neutral: enscribe 34/34, cli 133/133, vocab 52/52, empty test/fixtures
diff (the sweep touched only comments and documentation).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
