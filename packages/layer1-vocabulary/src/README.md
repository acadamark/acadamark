# src/

Custom-element registration files will live here. These are mostly one-liners — the Layer 1 elements are semantic markers, not interactive components, so registration is minimal JS.

Not yet implemented. See `../SPEC.md` for the vocabulary specification.
