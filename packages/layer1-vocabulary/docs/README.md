# docs/

Per-element reference pages will live here, one file per element (e.g., `elements/article.md`, `elements/float.md`). These are the second-draft deliverable: each element gets its allowed attributes specified, with JATS as the primary reference.

Not yet written. See `../SPEC.md` for the current vocabulary specification.
